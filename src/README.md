This folder is reserved for reusable analytical functions if the project is expanded beyond the Streamlit dashboard.
The current dashboard keeps the core transformation logic in `app.py` for simple deployment.
